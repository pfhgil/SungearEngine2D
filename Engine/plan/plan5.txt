план на 5 пунктов от 10.06.22

1) сделать компонент ScriptComponent, а также класс Script
выполнено: нет

2) сделать отображение всех public @InspectorView полей в инспекторе
выполнено: нет

3) сделать drag and drop на эти поля, если типы классов перетаскиваемого объекта и объекта в поле совпадают
выполнено: нет

4) сделать класс ScriptSystem, который будет вызывать методы update и deltaUpdate в скриптах у объектов, если система активна и объект тоже
выполнено: нет

5) сделать сериализацию скриптов (путей до них)
выполнено: нет

-Xmx2048м
-Xss256k
-XX:+UseG1GC
-XX:InitiatingHeapOccupancyPercent=65
-XX:G1HeapRegionSize=2
-XX:MaxGCPauseMillis=100