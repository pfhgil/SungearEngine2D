план создания 2д движка:
1) подобрать нормальный стиль интерфейса
выполнено: да

2) реализовать макет 3-ех панелей (слева - дерево сцены, справа - инспектор, снизу - панель ресурсов, которая будет делиться еще на две панели:
    2.1) панель дерева проекта
    2.2) панель файлов текущей папки
    )
выполнено: да

3) реализовать верхний тулбар. кнопки:
    3.1) File
        3.1.1) New...
            3.1.1.1) Project - диалоговое окно
            - далее разделитель -
            (все файлы создаются в текущей папке в панели файлов в нижней панели)
            3.1.1.2) Java file - диалоговое окно
            3.1.1.3) Text file - диалоговое окно
        3.1.2) Open...
            3.1.2.1) Project - диалоговое окно
        3.1.3) Close project
        3.1.4) Project settings - диалоговое окно
        3.1.5) Build project - диалоговое окно
        3.1.5) Exit - диалоговое окно
    3.2) Objects
        3.2.1) New...
            3.2.1.1) Object2D
            3.2.1.2) Camera2D
            3.2.1.3) Instancing object
            3.2.1.4) AtlasDrawing object
    3.3) UI
        3.3.1) New...
            3.3.1.1) Text
            3.3.1.2) Progress Bar
            3.3.1.3) Button
    3.4) Scene
        3.4.1) Create - диалоговое окно
        3.4.2) Delete - диалоговое окно
    3.6) Engine
        3.6.1) Settings - диалоговое окно
выполнено: да

4) реализовать создание проекта (при создании автоматически будет появляться папка с именем проекта в заданном пути, а также папки Scripts и Resources)
выполнено: да

5) реализовать дерево проекта в нижней панели
выполнено: да

6) реализовать панель файлов текущей папки (с надписями названия файлов, иконкой файла)
выполнено: да

7) реализовать перемещение по дереву проекта
выполнено: да

8) добавить якорь камеры, который будет появляться в (0.0, 0.0) при создании проекта и прикрепить камеру к якорю
выполнено: да

9) реализовать сетку размером каждой клеточки 50 на 50 (
    200 объектов по позиции x = -5000 y = каждый вниз (начиная от y = 4950 и заканчивая y = -4950) на -50 растянуты по x до 5000 и по y до 5 (т.е. scale = (100, 0.05)) и
    200 объектов по позиции x = каждый вправо (начиная от x = -4950 и заканчивая x = 4950) на +50 растянуты по y до -5000 и по x до 5 (т.е. scale = (0.05, -100))
    (возможно придется сделать линиями (т.е. через GL_LINES))
    )
выполнено: да

10) реализовать управление камерой:
    10.1) колесико мыши: удаление/приближение камеры
    10.2) нажатие - перемещение - отжатие левой кнопки мыши: перемещение камеры
    пока хватит
выполнено: да

после добавления всего этого списка, дополнить